/*
	Simple procedural pattern for the python demo.
	This is NOT an example on how to write a shader.
*/
shader waves(
	output color outColor = 0 )
{
	outColor = 0.5 + 0.5 * sin(sqrt(pow(u-0.5,2) + pow(v-0.5,2))*40);
}
