/* Basic material for demo purposes. */
surface matte(
        float Kd = 1,
        color Cs = 1,
        color Os = 1 )
{
        Ci = Kd * Cs * diffuse(N);
        Ci += (1 - Os) * transparent();
        Ci += Cs * debug("surfacecolor");
}
